# =============================================================================
# CRK Edge Jetson .env 예시 (한글 주석)
# =============================================================================
# 이 파일 내용은 Jetson의 .env에 그대로 넣어 쓰는 예시입니다.
# 실제 비밀번호, API 키, 토큰, 엔진 파일명은 현장 값으로 바꿔야 합니다.
# 주석은 붙여넣기 호환성을 위해 변수 바로 윗줄에 둡니다.
# =============================================================================

# 실행 환경입니다. 운영 Jetson에서는 production을 사용합니다.
NODE_ENV=production

# 외부 공통 API가 바인딩할 호스트입니다. 로컬에서만 접근하려면 127.0.0.1을 사용합니다.
API_HOST=127.0.0.1

# 형제 서비스에서 쓰는 API 인증 키입니다. 운영에서는 반드시 임의의 강한 값으로 바꿉니다.
API_KEY=change-this-to-secure-key

# Node.js 로그인 사용자 ID입니다. 토큰 기반 운영이면 비워둘 수 있습니다.
USER_ID=

# Node.js 로그인 비밀번호입니다. 토큰 기반 운영이면 비워둘 수 있습니다.
USER_PASSWORD=

# Node.js 서비스가 쓰는 JWT 토큰입니다. 운영 방식에 맞게 채웁니다.
JWT_TOKEN=

# JWT access token 또는 보조 토큰입니다. 운영 방식에 맞게 채웁니다.
JWT_TOKEN_AT=

# IO Board 서비스 URL입니다. 로드셀/문 상태를 받는 서비스입니다.
IO_BOARD_URL=http://localhost:8000

# Camera driver 서비스 URL입니다. 녹화 시작/종료와 AVI 파일 생성을 담당합니다.
CAMERA_DRIVER_URL=http://localhost:8003

# Node.js 오케스트레이터 URL입니다. model-service가 판단 결과를 보낼 대상입니다.
NODEJS_URL=http://localhost:8888

# model-service URL입니다. 다른 서비스가 상품 판단 API를 호출할 때 사용합니다.
PRODUCT_JUDGE_URL=http://localhost:8002

# MQTT client 서비스 URL입니다.
MQTT_CLIENT_URL=http://localhost:8006

# CHAI 클라우드 REST API 주소입니다. 현장/운영 API 주소로 바꿉니다.
REST_API=https://api.example.com

# MQTT 브로커 호스트입니다.
MQTT_BROKER_HOST=localhost

# MQTT 브로커 포트입니다.
MQTT_BROKER_PORT=1883

# Python 서비스용 MQTT 사용자명입니다. 인증이 없으면 비워둡니다.
MQTT_CLIENT_USERNAME=

# Python 서비스용 MQTT 비밀번호입니다. 인증이 없으면 비워둡니다.
MQTT_CLIENT_PASSWORD=

# Node.js 서비스용 MQTT URL입니다.
MQTT_URL=mqtt://localhost:1883

# Node.js 서비스용 MQTT 사용자명입니다. 인증이 없으면 비워둡니다.
MQTT_USER=

# Node.js 서비스용 MQTT 비밀번호입니다. 인증이 없으면 비워둡니다.
MQTT_PASS=

# 매장/사업부 식별자입니다. 서버 등록 값과 맞춰야 합니다.
DIVISION_IDX=DIV001

# 장비 식별자입니다. 서버 등록 값과 맞춰야 합니다.
DEVICE_IDX=DEV001

# IO Board 시리얼 포트입니다. Jetson에서는 /dev/ttyUSB0 또는 /dev/ttyTHS0 계열을 확인합니다.
IO_BOARD_PORT=/dev/ttyUSB0

# IO Board 시리얼 통신 속도입니다.
IO_BOARD_BAUDRATE=38400

# 로드셀 필터 방식입니다. none, exponential, kalman 중 하나를 사용합니다.
LOADCELL_FILTER_METHOD=exponential

# exponential 필터 강도입니다. 0에 가까울수록 더 부드럽고, 1에 가까울수록 원본에 가깝습니다.
LOADCELL_FILTER_ALPHA=0.3

# IO Board/Node 쪽에서 loadcell.change 이벤트를 낼 최소 무게 변화량(g)입니다.
LOADCELL_CHANGE_THRESHOLD=50

# 이벤트 임계값을 raw와 filtered 중 어느 값에 적용할지 정합니다.
LOADCELL_THRESHOLD_SCOPE=filtered

# model-service API 바인딩 호스트입니다. 같은 Jetson 내 서비스 접근이면 0.0.0.0을 사용합니다.
MODEL__API__HOST=0.0.0.0

# model-service API 포트입니다.
MODEL__API__PORT=8002

# model-service 로그 레벨입니다. 운영 기본은 info, 상세 추적은 debug입니다.
MODEL__API__LOG_LEVEL=info

# 종료 시 요청 정리를 기다리는 시간(초)입니다.
MODEL__API__TIMEOUT_GRACEFUL_SHUTDOWN=30

# 냉장/냉동 선택입니다. refrigerated는 냉장, freezer는 냉동입니다.
# freezer도 Camera가 보내는 zone loadcells를 사용하고 상품 판단은 vision-first로 처리합니다.
MODEL__MACHINE__CABINET_TYPE=refrigerated

# TensorRT YOLO 엔진 파일 경로입니다. 실제 Jetson에 존재하는 .engine 파일명으로 맞춥니다.
MODEL__VISION__YOLO_MODEL_PATH=models/0204_morning.engine
MODEL__VISION__YOLO_INTERNAL_CONF_THRESHOLD=0.01
MODEL__VISION__LOG_ENGINE_CLASSES=off

# Runtime catalog policy. product_eng_name이 공식 class key입니다.
# 전환 기간에는 engine class와 일치하는 name/legacy product_name도 호환합니다.
MODEL__CATALOG__SOURCE_POLICY=node_first
MODEL__CATALOG__PRODUCT_NAME_FALLBACK_ENABLED=true
MODEL__CATALOG__STATIC_VALIDATION_ENABLED=false

# YOLO hand class id입니다. 보통 0입니다.
MODEL__VISION__HAND_CLASS_ID=0
MODEL__VISION__HAND_CONFIDENCE_THRESHOLD=0.40

# hand path와 상품 bbox를 연동할 때 쓰는 최대 거리(px)입니다.
MODEL__VISION__MAX_DISTANCE_PX=100.0

# 최종 후보로 유지할 최대 상품 후보 수입니다.
MODEL__VISION__TOP_K=10

# top camera YOLO confidence 임계값입니다. 낮출수록 더 많이 잡고 후단 필터에 맡깁니다.
MODEL__VISION__TOP_CONFIDENCE_THRESHOLD=0.70

# side camera YOLO confidence 임계값입니다.
MODEL__VISION__SIDE_CONFIDENCE_THRESHOLD=0.70

# 전체 처리 프레임 중 해당 class가 등장해야 하는 최소 비율입니다.
MODEL__VISION__MIN_VOTE_RATIO=0.02

# 짧은 영상에서 비율 조건 대신 후보로 살릴 최소 감지 횟수입니다.
MODEL__VISION__MIN_VOTE_COUNT=1

# motion_passed=true가 되기 위한 bbox 중심 이동 최소값(px)입니다. 현재 운영 목표는 10px입니다.
MODEL__VISION__MOTION_MIN_DISPLACEMENT_PX=10.0

# freezer 전용 dual-top filtering입니다. freezer + dual_top_proxy에서 top_middle/top_side 모두 center_y >= 240만 후보로 씁니다.
MODEL__VISION__FREEZER_MIN_VOTE_RATIO=0.08
MODEL__VISION__FREEZER_MIN_VOTE_COUNT=3
MODEL__VISION__FREEZER_MOTION_MIN_DISPLACEMENT_PX=12.0
MODEL__VISION__FREEZER_LOWER_ROI_Y_SPLIT=240.0
MODEL__VISION__FREEZER_MIN_EXIT_PATH_VOTES=3

# Camera layout. dual_top_proxy maps videos.top to top_middle and videos.side to top_side.
MODEL__VISION__CAMERA_LAYOUT=legacy_top_side

# side camera에서 유효한 왼쪽 영역의 최대 center_x입니다. 이보다 오른쪽이면 필터링됩니다.
MODEL__VISION__SIDE_ROI_X_MAX=400.0
MODEL__VISION__SIDE_ROI_SOFT_MARGIN_PX=5.0

# ROI rescue는 side camera 오른쪽 영역에서 걸러진 후보를 되살리는 예외 경로입니다. 정지 상품이 올라오지 않도록 motion을 요구합니다.
MODEL__VISION__ROI_RESCUE_REQUIRE_MOTION=true

# ROI rescue 후보의 평균 center_x가 side ROI 기준보다 이 값(px) 이상 오른쪽이면 후보로 쓰지 않습니다.
MODEL__VISION__ROI_RESCUE_MAX_OVER_LIMIT_PX=0.0

# top camera의 하단 ROI 필터를 켤지 정합니다. delta가 0이 아니면 적용됩니다.
MODEL__VISION__TOP_ROI_ENABLED=true

# top camera ROI 기준 y좌표입니다. 제거(delta<0)와 반납(delta>0) 모두 하단을 사용합니다.
MODEL__VISION__TOP_ROI_Y_SPLIT=240.0

# YOLO 입력 전 left crop 기준 폭입니다.
MODEL__VISION__CROP_WIDTH=480

# top camera 전처리 방식입니다. left는 640x480에서 왼쪽 480x480만 사용합니다.
MODEL__VISION__TOP_CROP_POLICY=left

# side camera 전처리 방식입니다. left는 640x480에서 왼쪽 480x480만 사용합니다.
MODEL__VISION__SIDE_CROP_POLICY=left

# top crop policy가 offset일 때 쓸 x offset입니다. left면 주석 상태로 둡니다.
# MODEL__VISION__TOP_CROP_X_OFFSET=0

# side crop policy가 offset일 때 쓸 x offset입니다. left면 주석 상태로 둡니다.
# MODEL__VISION__SIDE_CROP_X_OFFSET=0

# trigger trace에 제한된 all-class 진단을 남길지 정합니다. 현장 디버깅에는 true가 유용합니다.
MODEL__VISION__DIAGNOSTIC_ALL_CLASS_TRACE=true

# all-class 진단에 사용할 카메라별 최대 프레임 수입니다.
MODEL__VISION__DIAGNOSTIC_TRACE_MAX_FRAMES=5

# 낮은 confidence 감지를 weight-gated rescue 후보로 남길지 정합니다.
MODEL__VISION__THRESHOLD_RESCUE_ENABLED=true

# threshold rescue 후보의 confidence 상한입니다.
MODEL__VISION__THRESHOLD_RESCUE_CONFIDENCE_CAP=0.18

# threshold rescue 후보에도 motion 증거를 요구할지 정합니다.
MODEL__VISION__THRESHOLD_RESCUE_REQUIRE_MOTION=true

# trace에 보관할 threshold rescue 후보 최대 수입니다.
MODEL__VISION__THRESHOLD_RESCUE_MAX_CANDIDATES=20

# motion이 없는 저신뢰 후보도 매우 타이트한 무게 일치로 rescue할지 정합니다.
MODEL__VISION__WEIGHT_RESCUE_NO_MOTION_ENABLED=true

# no-motion weight rescue에 필요한 최소 raw vote 수입니다.
MODEL__VISION__WEIGHT_RESCUE_NO_MOTION_MIN_RAW_VOTES=8

# no-motion weight rescue의 최대 잔차(g)입니다. 정지 상품 오검출 방지를 위해 작게 둡니다.
MODEL__VISION__WEIGHT_RESCUE_NO_MOTION_MAX_RESIDUAL_GRAMS=2.0

# top camera vote 가중치입니다.
MODEL__VISION__TOP_WEIGHT=0.60

# side camera vote 가중치입니다.
MODEL__VISION__SIDE_WEIGHT=0.40

# top/side 둘 다 같은 class를 잡았을 때 추가되는 보너스입니다.
MODEL__VISION__COMMON_CLASS_BONUS=0.2

# top camera에만 잡힌 후보의 가중치입니다.
MODEL__VISION__TOP_ONLY_WEIGHT=0.60

# side camera에만 잡힌 후보의 가중치입니다.
MODEL__VISION__SIDE_ONLY_WEIGHT=0.40

# FFmpeg top camera gamma 보정값입니다. 1.0은 원본입니다.
MODEL__VISION__FFMPEG_TOP_GAMMA=1.2

# FFmpeg top camera contrast 보정값입니다. 1.0은 원본입니다.
MODEL__VISION__FFMPEG_TOP_CONTRAST=1.2

# FFmpeg side camera gamma 보정값입니다. 1.0은 원본입니다.
MODEL__VISION__FFMPEG_SIDE_GAMMA=1.0

# FFmpeg side camera contrast 보정값입니다. 1.0은 원본입니다.
MODEL__VISION__FFMPEG_SIDE_CONTRAST=1.0

# strict weight matcher에서 쓰는 퍼센트 허용오차입니다. relaxed 계열 계산에 사용됩니다.
MODEL__WEIGHT__IDENTITY_POLICY=vision_first
MODEL__WEIGHT__FUSION_VISION_WEIGHT=0.65
MODEL__WEIGHT__FUSION_LOADCELL_WEIGHT=0.25
MODEL__WEIGHT__FUSION_COUNT_WEIGHT=0.10
MODEL__WEIGHT__TOLERANCE_PERCENT=0.08

# 일반 strict weight matching의 고정 허용오차(g)입니다. 같은상품 반복 전용 허용오차와 별개입니다.
MODEL__WEIGHT__TOLERANCE_GRAMS=5.0

# 다품목 조합에서 각 상품 후보가 넘어야 하는 최소 vision confidence입니다. 현장 후보 0.2~0.3대를 살리기 위해 0.18을 사용합니다.
MODEL__WEIGHT__MULTI_KIND_MIN_CONFIDENCE=0.18

# vision rescue 전용 고정 허용오차(g)입니다.
MODEL__WEIGHT__RESCUE_TOLERANCE_GRAMS=5.0

# 같은상품 반복 수량 판단에서 개당 허용할 잔차(g)입니다. x2면 총 10g까지 허용됩니다.
MODEL__WEIGHT__SAME_PRODUCT_COUNT_TOLERANCE_GRAMS=5.0

# 같은상품 반복 수량의 최대 개수입니다. 현재 요구사항은 같은상품 x8까지입니다.
MODEL__WEIGHT__SAME_PRODUCT_MAX_COUNT=8

# loadcell segment 1개를 손으로 한 번 잡은 동작으로 보고, segment당 상품 수를 제한합니다.
# segment가 2개면 기본값 3 기준으로 전체 최대 6개까지 허용합니다.
MODEL__WEIGHT__MAX_ITEMS_PER_SEGMENT=3

# strict/relaxed 실패 후 감지된 단일 상품 근사 fallback을 켤지 정합니다.
MODEL__WEIGHT__DETECTED_SINGLE_FALLBACK_ENABLED=true

# 감지된 단일 상품 fallback의 최대 잔차(g)입니다.
MODEL__WEIGHT__DETECTED_SINGLE_FALLBACK_TOLERANCE_GRAMS=8.0

# 감지된 단일 상품 fallback에 필요한 최소 vote 수입니다.
MODEL__WEIGHT__DETECTED_SINGLE_FALLBACK_MIN_VOTES=4

# freezer 전용 판단 설정입니다. 무게 잔차는 후보 reject가 아니라 confidence 근접 후보 간 tie-break로만 씁니다.
MODEL__WEIGHT__FREEZER_CONFIDENCE_TIE_BAND=0.08
MODEL__WEIGHT__FREEZER_MULTI_MIN_CONFIDENCE=0.45
MODEL__WEIGHT__FREEZER_WEIGHT_TOLERANCE_GRAMS=15.0

# 판단할 최소 무게 변화량(g)입니다. 이보다 작으면 no-detection 또는 진단 경로가 됩니다.
MODEL__WEIGHT__MIN_WEIGHT_CHANGE=5.0

# relaxed 조합 계산의 최대 총 개수입니다.
MODEL__WEIGHT__MAX_COMBINATION_SIZE=5

# strict weight-first matching을 사용할지 정합니다.
MODEL__WEIGHT__STRICT_MODE=true

# strict mismatch 후 relaxed fallback으로 내려갈지 정합니다.
MODEL__WEIGHT__STRICT_MODE_FALLBACK=true

# strict 조합 검색의 최대 총 상품 개수입니다. 시나리오 A2+B2/A5를 위해 5를 사용합니다.
MODEL__WEIGHT__MAX_COMBINATION_ITEMS=5

# strict 조합 검색의 최대 상품 종류 수입니다. A+B+C를 위해 3을 사용합니다.
MODEL__WEIGHT__MAX_COMBINATION_KINDS=3

# 상품 하나가 조합에서 가질 수 있는 최대 개수입니다. 같은상품 x8보다 커야 합니다.
MODEL__WEIGHT__MAX_COUNT_PER_ITEM=10

# strict matcher가 탐색할 최대 조합 수입니다. 너무 크면 런타임이 늘 수 있습니다.
MODEL__WEIGHT__MAX_COMBINATIONS=200

# loadcell-only nearest fallback에서 1등과 2등 후보 사이에 필요한 최소 차이(g)입니다.
MODEL__WEIGHT__NEAREST_SINGLE_MARGIN_GRAMS=10.0

# trigger id 중복 제거 TTL(초)입니다.
MODEL__TRIGGER__DEDUP_TTL_SECONDS=5.0

# 중복 제거 캐시에 보관할 최대 trigger 수입니다.
MODEL__TRIGGER__DEDUP_MAX_SIZE=1000

# trigger worker queue 최대 크기입니다.
MODEL__TRIGGER__QUEUE_MAX_SIZE=20

# trigger 단계에서 비디오 처리를 시작할 최소 무게 변화량(g)입니다.
MODEL__TRIGGER__MIN_WEIGHT_CHANGE_GRAMS=5.0

# 낮은 무게 변화에서도 vision-only fallback을 시도할지 정합니다.
MODEL__TRIGGER__LOW_WEIGHT_VISION_FALLBACK=true

# 양수 delta(반납)는 YOLO를 생략하고 loadcell/session 경로로 처리할지 정합니다.
MODEL__TRIGGER__RETURN_VIDEO_SKIP_ENABLED=true

# 반납 delta는 짧은 first/last fallback을 바로 쓰지 않고 이 시간만큼 안정화를 기다립니다.
MODEL__TRIGGER__RETURN_STABILIZATION_WAIT_SECONDS=1.0

# true이면 반납 loadcell-only 커밋에는 stable start/end region이 필요합니다.
MODEL__TRIGGER__RETURN_STABILIZATION_REQUIRE_STABLE_REGIONS=true

# 나중에 들어온 반납 delta가 이전 제거 trigger를 상쇄하면 취소할지 정합니다.
MODEL__TRIGGER__BALANCED_EVENT_CANCEL_ENABLED=true

# 비싼 비디오 처리 시작 전 queue 취소 여부를 다시 확인할지 정합니다.
MODEL__TRIGGER__COOPERATIVE_CANCEL_ENABLED=true

# Same-zone loadcell trigger history window included in traces (seconds).
MODEL__TRIGGER__RAPID_SAME_ZONE_WINDOW_SECONDS=3.0

# 로드셀 stable region 계산에 사용할 window 크기입니다.
MODEL__LOADCELL__STABLE_WINDOW_SIZE=5

# stable window로 인정할 최대 표준편차(g)입니다.
MODEL__LOADCELL__STABILITY_THRESHOLD_GRAMS=15.0

# AVI 파일 준비를 기다릴 최대 시간(초)입니다.
MODEL__VIDEO__READY_MAX_WAIT_SECONDS=2.0

# AVI 파일 준비 확인 polling 간격(초)입니다.
MODEL__VIDEO__READY_POLL_INTERVAL_SECONDS=0.2

# async streaming video processing을 사용할지 정합니다. 운영에서는 true 권장입니다.
MODEL__ASYNC_STREAMING__ENABLED=true

# async frame queue 크기입니다.
MODEL__ASYNC_STREAMING__FRAME_QUEUE_SIZE=10

# 프레임 stride입니다. 2면 한 프레임 처리 후 한 프레임을 건너뜁니다.
MODEL__ASYNC_STREAMING__FRAME_STRIDE=2

# 충분한 vote가 모이면 조기 종료할지 정합니다.
MODEL__ASYNC_STREAMING__EARLY_TERMINATION_ENABLED=false

# 조기 종료를 사용할 때 필요한 vote 수입니다.
MODEL__ASYNC_STREAMING__EARLY_TERMINATION_VOTE_THRESHOLD=10

# async decode가 0 frame일 때 fallback retry를 시도할지 정합니다.
MODEL__ASYNC_STREAMING__ZERO_FRAME_RETRY_ENABLED=true

# trigger trace용 sample frame export를 켤지 정합니다. 디스크 사용량이 늘 수 있습니다.
MODEL__TRACE__SAMPLE_EXPORT_ENABLED=false

# sample export 시 카메라별 저장할 프레임 수입니다.
MODEL__TRACE__SAMPLE_COUNT_PER_CAMERA=3

# sample frame 저장 디렉터리입니다.
MODEL__TRACE__SAMPLE_EXPORT_DIR=logs/frame_samples

# trigger buffer TTL(초)입니다.
MODEL__BUFFER__TTL_SECONDS=300

# 동시에 보관할 trigger/session buffer 최대 수입니다.
MODEL__BUFFER__MAX_SESSIONS=100

# 만료 buffer 정리 주기(초)입니다.
MODEL__BUFFER__CLEANUP_INTERVAL_SECONDS=60

# door session 기능을 켤지 정합니다.
MODEL__DOOR_SESSION__ENABLED=true

# door session YAML 저장 디렉터리입니다.
MODEL__DOOR_SESSION__YAML_DIR=data/sessions

# door session timeout(초)입니다.
MODEL__DOOR_SESSION__SESSION_TIMEOUT_SECONDS=30.0

# door session basket/return 보정에서 쓰는 무게 허용오차(g)입니다.
MODEL__DOOR_SESSION__WEIGHT_TOLERANCE_GRAMS=5.0

# door session 최대 지속 시간(초)입니다.
MODEL__DOOR_SESSION__MAX_DURATION_SECONDS=600.0

# CLOSE 직후 첫 결과 확정을 기다리는 시간(초)입니다.
MODEL__DOOR_SESSION__CLOSE_INITIAL_WAIT_SECONDS=3.0

# CLOSE 이후 추가 polling 대기 시간(초)입니다.
MODEL__DOOR_SESSION__CLOSE_SUBSEQUENT_WAIT_SECONDS=1.0

# session YAML 파일 보관 기간(일)입니다.
MODEL__DOOR_SESSION__YAML_RETENTION_DAYS=7

# model-service가 Node.js에 judgment callback을 보낼 기본 URL입니다.
MODEL__NODEJS_URL=http://localhost:8888

# Node.js judgment callback endpoint 경로입니다.
MODEL__NODEJS_JUDGMENT_ENDPOINT=/api/sensor/judgment

# Camera 서비스 모드입니다. 운영 Jetson에서는 api를 사용합니다.
CAMERA_MODE=api

# Nvidia 하드웨어 경로 사용 여부입니다.
NVIDIA_MODE=true

# Camera capture width입니다.
CAMERA_RESOLUTION_WIDTH=640

# Camera capture height입니다.
CAMERA_RESOLUTION_HEIGHT=480

# Camera FPS입니다.
CAMERA_FPS=30

# Camera JPEG 품질입니다.
CAMERA_JPEG_QUALITY=80

# 공통 로그 레벨입니다.
LOG_LEVEL=INFO

# Card API 서버 host입니다.
CARD_API_HOST=0.0.0.0

# Card API 서버 port입니다.
CARD_API_PORT=5000

# CAT 단말 TCP 서버 host입니다.
CAT_HOST=0.0.0.0

# CAT 단말 TCP 서버 port입니다.
CAT_PORT=5001

# 결제/단말 통신 timeout(초)입니다.
COMM_TIMEOUT=30

# 결제 서비스 종료 timeout(초)입니다.
SHUTDOWN_TIMEOUT=10

# MongoDB 연결 문자열입니다.
MONGO_URI=mongodb://localhost:27017/chai

# MinIO endpoint host입니다.
MINIO_ENDPOINT=localhost

# MinIO port입니다.
MINIO_PORT=9000

# MinIO access key입니다. 운영에서는 변경해야 합니다.
MINIO_ACCESS_KEY=minioadmin

# MinIO secret key입니다. 운영에서는 변경해야 합니다.
MINIO_SECRET_KEY=minioadmin

# MinIO SSL 사용 여부입니다.
MINIO_USE_SSL=false

# 이미지 저장 bucket 이름입니다.
MINIO_BUCKET=chaiimage
